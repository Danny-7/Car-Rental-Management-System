{% extends 'base.html.twig' %}


{% block content %}

    <div class="container h-100" style="display: flex; justify-content: center">
        <a href="{{ path('cars') }}" class="btn btn-success my-auto">Voir nos voitures</a>
    </div>
{% endblock %}
